name: replace

on:
  push:
    branches: ['main']
  pull_request:
    branches: ['main']

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: sudo sed -i 's/^access_key_id/${{ access_key_id }}/g' index.js
  
